/**
 * @deprecated préférer `import theme from "./theme"` et `theme.colors`.
 * Conservé pour compatibilité : même objet que theme.colors.
 */
import theme from "./theme";

export default theme.colors;
